import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chennaihouseprices.app',
  appName: 'Tamil Nadu Property Price Predictor',
  webDir: 'build'
};

export default config;